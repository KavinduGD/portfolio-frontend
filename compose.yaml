name: portfolio

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.dev
      args:
        - VITE_BASE_URL=${VITE_BASE_URL}
    ports:
      - "5173:5173"
    develop:
      watch:
        - action: sync
          path: ./src
          target: /app/src
